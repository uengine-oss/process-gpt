# =====================================================================
# Process-GPT - Shared E2E orchestration entrypoint
# ---------------------------------------------------------------------
# Run from the repository root:
#   docker compose -f docker-compose.e2e.yml up -d
#   docker compose -f docker-compose.e2e.yml down -v       # full reset
#
# Compose auto-loads the repo-root `.env` for ${VAR} substitution.
#
# Hybrid Runtime Rule:
#   - This file boots ONLY stable infrastructure dependencies.
#   - Repository-owned application services (completion, frontend) run
#     directly from the working tree via the source-run helper scripts
#     under openspec/specs/<spec-name>/e2e/scripts/.
#   - The nginx `gateway` container is a thin reverse proxy that routes
#     browser traffic to the host source-run frontend/backend via
#     host.docker.internal.
#
# Only nondeterministic EXTERNAL boundaries are stubbed:
#   - mock-llm : OpenAI-compatible embedding stub (/v1/embeddings).
# =====================================================================

name: process-gpt-e2e

services:
  # ------------------------------------------------------------------
  # Supabase Postgres + pgvector (mem0/vecs documents)
  # ------------------------------------------------------------------
  db:
    image: supabase/postgres:15.8.1.060
    container_name: process-gpt-e2e-db
    command:
      - postgres
      - -c
      - config_file=/etc/postgresql/postgresql.conf
      - -c
      - log_min_messages=fatal
    environment:
      POSTGRES_HOST: /var/run/postgresql
      PGPORT: 5432
      POSTGRES_PORT: 5432
      PGPASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATABASE: postgres
      POSTGRES_DB: postgres
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXP: ${JWT_EXPIRY}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -h localhost"]
      interval: 5s
      timeout: 5s
      retries: 40
      start_period: 60s
    ports:
      - "54322:5432"
    volumes:
      - ./infra/volumes/db/realtime.sql:/docker-entrypoint-initdb.d/migrations/99-realtime.sql:ro
      - ./infra/volumes/db/webhooks.sql:/docker-entrypoint-initdb.d/init-scripts/98-webhooks.sql:ro
      - ./infra/volumes/db/roles.sql:/docker-entrypoint-initdb.d/init-scripts/99-roles.sql:ro
      - ./infra/volumes/db/jwt.sql:/docker-entrypoint-initdb.d/init-scripts/99-jwt.sql:ro
      - ./infra/volumes/db/_supabase.sql:/docker-entrypoint-initdb.d/migrations/97-_supabase.sql:ro
      - ./infra/volumes/db/logs.sql:/docker-entrypoint-initdb.d/migrations/99-logs.sql:ro
      - ./infra/volumes/db/pooler.sql:/docker-entrypoint-initdb.d/migrations/99-pooler.sql:ro
      - ./init.sql:/docker-entrypoint-initdb.d/init-scripts/100-init.sql:ro
      - e2e-db-data:/var/lib/postgresql/data

  # ------------------------------------------------------------------
  # Supabase Kong API gateway (browser reaches auth/rest through this)
  # ------------------------------------------------------------------
  kong:
    image: kong:2.8.1
    container_name: process-gpt-e2e-kong
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "54321:8000"
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /home/kong/kong.yml
      KONG_DNS_ORDER: LAST,A,CNAME
      KONG_PLUGINS: request-transformer,cors,key-auth,acl
      KONG_NGINX_PROXY_PROXY_BUFFER_SIZE: 160k
      KONG_NGINX_PROXY_PROXY_BUFFERS: 64 160k
      SUPABASE_ANON_KEY: ${ANON_KEY}
      SUPABASE_SERVICE_KEY: ${SERVICE_ROLE_KEY}
    volumes:
      - ./infra/volumes/api/kong.yml:/home/kong/temp.yml:ro
    entrypoint: bash -c 'eval "echo \"$$(cat ~/temp.yml)\"" > ~/kong.yml && /docker-entrypoint.sh kong docker-start'

  # ------------------------------------------------------------------
  # Supabase GoTrue (browser login / auth)
  # ------------------------------------------------------------------
  auth:
    image: supabase/gotrue:v2.177.0
    container_name: process-gpt-e2e-auth
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9999/health"]
      timeout: 25s
      interval: 5s
      retries: 20
      start_period: 30s
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      API_EXTERNAL_URL: ${API_EXTERNAL_URL}
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://supabase_auth_admin:${POSTGRES_PASSWORD}@db:5432/postgres
      GOTRUE_SITE_URL: ${SITE_URL}
      GOTRUE_URI_ALLOW_LIST: "*"
      GOTRUE_DISABLE_SIGNUP: "false"
      GOTRUE_JWT_ADMIN_ROLES: service_role
      GOTRUE_JWT_AUD: authenticated
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated
      GOTRUE_JWT_EXP: ${JWT_EXPIRY}
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_EXTERNAL_EMAIL_ENABLED: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "true"

  # ------------------------------------------------------------------
  # Supabase PostgREST
  # ------------------------------------------------------------------
  rest:
    image: postgrest/postgrest:v12.2.12
    container_name: process-gpt-e2e-rest
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: postgres://authenticator:${POSTGRES_PASSWORD}@db:5432/postgres
      PGRST_DB_SCHEMAS: public,storage,graphql_public
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"
    command: "postgrest"

  # ------------------------------------------------------------------
  # mock-llm: deterministic embedding stub (only external boundary)
  # ------------------------------------------------------------------
  mock-llm:
    image: python:3.12-slim
    container_name: process-gpt-e2e-mock-llm
    working_dir: /app
    volumes:
      - ./openspec/e2e/scripts:/app:ro
    command: ["python", "-u", "mock_llm.py"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health', timeout=3).read()"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    ports:
      - "8899:8080"

  # ------------------------------------------------------------------
  # gateway: thin nginx reverse proxy that fronts source-run apps
  # ------------------------------------------------------------------
  gateway:
    image: nginx:1.27-alpine
    container_name: process-gpt-e2e-gateway
    ports:
      - "8088:8088"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./openspec/specs/completion_process-definition-search/e2e/docker/nginx.e2e.conf:/etc/nginx/nginx.conf:ro
      # Serve the source-mapped Vite dist bundle directly. The Vite preview
      # server lacks SPA history fallback, so nginx + try_files is the
      # smallest infrastructure addition that lets Playwright reach
      # /auth/login and any other SPA route.
      - ./services/frontend/dist:/frontend-dist:ro

  # ------------------------------------------------------------------
  # db-seed: one-shot seeding for users + vecs.documents
  # ------------------------------------------------------------------
  db-seed:
    image: postgres:16-alpine
    container_name: process-gpt-e2e-db-seed
    depends_on:
      db:
        condition: service_healthy
      auth:
        condition: service_healthy
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./openspec/specs/completion_process-definition-search/e2e/seed_files/e2e_seed.sql:/seed/e2e_seed.sql:ro
    entrypoint:
      - sh
      - -c
      - |
        until pg_isready -h db -U postgres >/dev/null 2>&1; do sleep 2; done
        psql "postgres://postgres:$$PGPASSWORD@db:5432/postgres" -v ON_ERROR_STOP=1 -f /seed/e2e_seed.sql
        echo "[db-seed] done"

volumes:
  e2e-db-data:
