# Copy to .env and update secret values.
# 이 .env는 docker-infra(순수 인프라: Supabase 스택 + Apache AGE + neo4j +
# LiteLLM)에서만 쓴다. 앱 서비스(frontend/completion/... services/<name>)는
# 이 폴더와 무관하게 각자의 .env를 따로 관리한다 — 단, ANON_KEY/SERVICE_ROLE_KEY/
# JWT_SECRET/SUPABASE_URL(=Kong 주소) 값은 아래와 동일하게 맞춰야 연동된다.

LITELLM_PORT=4010
LITELLM_DB_PORT=5432
LITELLM_DB_PASSWORD=litellm
LITELLM_NUM_WORKERS=1
LITELLM_MASTER_KEY=change-this-master-key
OPENROUTER_API_KEY=

# Kong(API Gateway) 외부 접속 주소. 이 머신 밖에서 접속할 경우 <localhost>를
# 실제 IP/호스트로 바꾼다 (예: http://192.168.1.10:54321).
API_EXTERNAL_URL=http://localhost:54321
SITE_URL=http://localhost:5199
SUPABASE_URL=http://localhost:54321
SUPABASE_PUBLIC_URL=http://localhost:54321
# 같은 docker-infra 네트워크 안(컨테이너 간)에서 Kong에 접속할 때 쓰는 주소.
SUPABASE_INTERNAL_URL=http://kong:8000

# =====================================================================
# SUPABASE DEV DEFAULTS (Supabase self-hosting reference values)
# ---------------------------------------------------------------------
# JWT_SECRET은 ANON_KEY/SERVICE_ROLE_KEY JWT를 서명하는 32자 이상 문자열이다.
# 이 세 값은 서로 짝이 맞아야 한다 — 바꾸려면 함께 재발급할 것
# (https://supabase.com/docs/guides/self-hosting#api-keys).
# 아래 값들은 로컬 개발용으로 바로 쓸 수 있다. 운영 배포 전엔 반드시 교체.
# =====================================================================
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJhbm9uIiwKICAgICJpc3MiOiAic3VwYWJhc2UtZGVtbyIsCiAgICAiaWF0IjogMTY0MTc2OTIwMCwKICAgICJleHAiOiAxNzk5NTM1NjAwCn0.dc_X5iR_VP_qT0zsiyj_I_OZ2T9FtRU2BBNWN8Bu4GE
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJzZXJ2aWNlX3JvbGUiLAogICAgImlzcyI6ICJzdXBhYmFzZS1kZW1vIiwKICAgICJpYXQiOiAxNjQxNzY5MjAwLAogICAgImV4cCI6IDE3OTk1MzU2MDAKfQ.DaYlNEoUrrEn2Ig7tqibS-PHK5vgusbcbo7X36XVt4Q
JWT_EXPIRY=3600
SECRET_KEY_BASE=UpNVntn3cDxHJpq99YMc1T1AQgQpc8kfYTuRgBiYa15BLrx8etQoXz3gZv1/u2oq
PGRST_DB_SCHEMAS=public,storage,graphql_public

STUDIO_PORT=3001
KONG_HTTP_PORT=54321
KONG_HTTPS_PORT=8443

POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password
# db 컨테이너에 호스트에서 직접(psql/DBeaver) 접속할 때 쓰는 포트.
# litellm-db가 이미 호스트 5432를 쓰므로 다른 값을 쓴다.
POSTGRES_HOST_PORT=54322

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_SENDER_NAME=Process-GPT
ENABLE_EMAIL_SIGNUP=true
ENABLE_ANONYMOUS_USERS=false
ENABLE_EMAIL_AUTOCONFIRM=false
ENABLE_PHONE_SIGNUP=false
ENABLE_PHONE_AUTOCONFIRM=false
DISABLE_SIGNUP=false
ADDITIONAL_REDIRECT_URLS=
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify
MAILER_SUBJECTS_CONFIRMATION=Confirm your email
MAILER_SUBJECTS_RECOVERY=Reset your password
MAILER_SUBJECTS_INVITE=You are invited
MAILER_SUBJECTS_EMAIL_CHANGE=Confirm email change
MAILER_TEMPLATES_CONFIRMATION=
MAILER_TEMPLATES_RECOVERY=
MAILER_TEMPLATES_INVITE=
MAILER_TEMPLATES_EMAIL_CHANGE=

STUDIO_DEFAULT_ORGANIZATION=default
STUDIO_DEFAULT_PROJECT=process-gpt
LOGFLARE_API_KEY=your-super-secret-and-long-logflare-key
LOGFLARE_PRIVATE_ACCESS_TOKEN=your-super-secret-and-long-logflare-private-token
IMGPROXY_ENABLE_WEBP_DETECTION=true
FUNCTIONS_VERIFY_JWT=false

NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
NEO4J_AUTH=neo4j/password
NEO4J_PLUGINS=[]

KONG_TRUSTED_IPS=0.0.0.0/0,::/0
KONG_REAL_IP_FROM=0.0.0.0/0,::/0
KONG_REAL_IP_HEADER=X-Forwarded-For

# Studio에서 쓰는 AI 어시스턴트용(선택). 비워두면 그 기능만 비활성.
OPENAI_API_KEY=
