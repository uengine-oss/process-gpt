# Compose override that re-runs the completion service under coverage.py.
# Apply alongside the root compose file:
#   docker compose -f docker-compose.e2e.yml \
#       -f openspec/specs/completion_agent-memory-chat/e2e/docker/coverage.override.yml \
#       up -d --build completion
# Then re-run Playwright, signal the uvicorn process with USR2 to flush
# coverage, and finally combine + report inside the container.
services:
  completion:
    command:
      - sh
      - -c
      - "pip install --quiet coverage && cd /app && coverage erase && \
         exec coverage run --parallel-mode --concurrency=thread \
           --source=. \
           -m uvicorn main:app --host 0.0.0.0 --port 8000"
    volumes:
      - ../../openspec/specs/completion_agent-memory-chat/e2e/results/backend-coverage:/coverage
    environment:
      COVERAGE_FILE: /coverage/.coverage
